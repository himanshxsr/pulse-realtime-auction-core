{
  "name": "pulse-realtime-auction-core",
  "version": "1.0.0",
  "private": true,
  "description": "High-concurrency, financial-grade real-time bidding engine",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --noEmit --project tsconfig.base.json"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@types/node": "^20.11.0",
    "socket.io-client": "^4.8.3",
    "typescript": "^5.4.2"
  }
}
